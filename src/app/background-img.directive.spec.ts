import { BackgroundImgDirective } from './background-img.directive';

describe('BackgroundImgDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundImgDirective();
    expect(directive).toBeTruthy();
  });
});
